package com.CPT.Utils;

import java.util.Properties;

import javax.mail.Authenticator;
import javax.mail.Message;
import javax.mail.PasswordAuthentication;
import javax.mail.Session;
import javax.mail.internet.InternetAddress;
import javax.mail.internet.MimeMessage;

public class Email {
	
	public static void name(String [] args) {
		final String from  = "adssfghjkjhhdh@gmail.com";
		final String password = "cvxbcvvmnvnmbm";
		//khai báo
		final String to = "la123este@gmail.com";
		Properties properties = new Properties();
		properties.put("mail.smtp.host"	, "smtp.gmail.com");//SMTP HOST
		properties.put("mail.smtp.port"	, "587");//TLS 587 SSL 465
		properties.put("mail.smtp.auth"	, "true");
		properties.put("mail.smtp.starttls.enable", "true");
		//tạo authen
		Authenticator auth = new Authenticator() {

			@Override
			protected PasswordAuthentication getPasswordAuthentication() {
				return new PasswordAuthentication(from, password);
			}
            
        };
        //tạo phiên làm việc
        Session session = Session.getInstance(properties, auth);
        
        final String to = "la123este@gmail.com";
        //tạo tin nhắn
        Message msg = new MimeMessage(session);
        try {
        	msg.addHeader("Content-type", "text/html; charset=UTF-8");
			msg.setFrom(from);
			msg.setRecipients(Message.RecipientType.TO, toAddresses);
		} catch (Exception e) {
			// TODO: handle exception
		}
        
        InternetAddress[] toAddresses = { new InternetAddress(recipientEmail) };
       
        msg.setSubject(subject);
        msg.setSentDate(new Date());
        msg.setText(message);
	}
}
